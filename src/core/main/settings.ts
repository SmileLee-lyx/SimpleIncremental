export enum SignSetting {
    DEFAULT,
    ALWAYS,
    NEVER,
}

export enum AutoSaveSetting {
    EVERY_30_SEC,
    EVERY_5_MIN,
    NEVER,
}

export enum AlertId {
    HIDE_SIGN,
    SHIFT,
    B_UNLOCK,
}

export enum BuyMode {
    BUY_ONE,
    BUY_TEN,
    BUY_MAX,
}